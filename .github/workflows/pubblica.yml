# Pubblica automaticamente il sito su GitHub Pages a ogni push sul ramo principale.
# Da attivare una sola volta: Impostazioni del repository > Pages > Source: GitHub Actions
name: Pubblica su GitHub Pages

on:
  push:
    branches:
      - main
      - claude/il-rustico-landing-page-ngmudy
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  pubblica:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - id: deploy
        uses: actions/deploy-pages@v4
